"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const path = __importStar(require("path"));
const fs_1 = require("fs");
/**
 * Check whether Dotty IDE is enabled on this project
 *
 * @param workspaceRoot the workspace root, if defined
 */
function checkDottyIde(workspaceRoot) {
    if (workspaceRoot) {
        const dottyIdeArtifactPath = path.join(workspaceRoot, ".dotty-ide-artifact");
        return {
            enabled: fs_1.existsSync(dottyIdeArtifactPath),
            path: dottyIdeArtifactPath,
        };
    }
    return { enabled: false };
}
exports.checkDottyIde = checkDottyIde;
