"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const getJavaOptions_1 = require("./getJavaOptions");
const path = __importStar(require("path"));
function getJavaConfig({ workspaceRoot, javaHome, extensionPath, customRepositories = [], }) {
    const javaOptions = getJavaOptions_1.getJavaOptions(workspaceRoot);
    const javaPath = path.join(javaHome, "bin", "java");
    const coursierPath = path.join(extensionPath, "./coursier");
    const extraEnv = {
        COURSIER_REPOSITORIES: customRepositories.join("|"),
    };
    return {
        javaOptions,
        javaPath,
        coursierPath,
        extraEnv,
    };
}
exports.getJavaConfig = getJavaConfig;
