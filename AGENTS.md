# AGENTS.md

- This is a `yadm`-managed dotfiles repo.
- Files live in `$HOME`; use `yadm add` and `yadm rm` to track changes.
- Keep changes minimal and avoid touching unrelated files.
- Current managed config: `~/.config/opencode/opencode.jsonc`.
